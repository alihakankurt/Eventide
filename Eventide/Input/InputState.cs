namespace Eventide.Input;

/// <summary>
/// An enum that declares states of <see cref="Key"/> and <see cref="Button"/>.
/// </summary>
public enum InputState
{
    Up = 0,
    Down = 1,
    Released = 2,
    Pressed = 3,
    StateCount = 4,
}
